{
  "name": "solvingzero-compare-plans-exercise",
  "version": "1.0.0",
  "private": true,
  "description": "SolvingZero take-home: fetch real CDR electricity plans and estimate + rank their annual cost for a real household usage profile.",
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "verify": "turbo run verify",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules apps/*/node_modules packages/*/node_modules .turbo"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "turbo": "^2.10.8",
    "typescript": "^5.6.3"
  }
}
